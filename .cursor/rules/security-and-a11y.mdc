# セキュリティとアクセシビリティ

## セキュリティ
- 依存は定期更新（minor/patch を許容）。重大脆弱性は優先修正
- 本番での CSP 設定を推奨（`default-src 'self'` を基本に最小許可）
- 不要スクリプトの除去と遅延読み込みで攻撃面を縮小

## アクセシビリティ
- 画像には `alt` を必須
- ナビゲーション要素には適切な `aria-*` を付与
- コントラストと可読性を確保（`glass` の背景は既に濃色テキストで調整）

## 将来拡張
- Analytics を導入する場合は IP 匿名化、同意フロー、遅延/最小収集を厳守

